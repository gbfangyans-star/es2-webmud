# Old Pine integration round 5

- Began real LPC staging under `/custom/oldpine_recovered/room` rather than report-only archaeology.
- Staged npath1, npath2, npath3 from the recovered historical source. Only path remapping and WebHUD map metadata were added.
- Historical npath1 north exit to `/d/snow/eroad3` is intentionally held until the user-approved branch connection is made; it is not treated as a lineage requirement.
- Historical npath3 east exit is intentionally held until `clearing.c` and its dependent east/south/tree branches are staged, preventing a dangling playable exit.
- No fabricated room prose, NPC, object, skill, or mechanic was introduced.
- This is a quarantine/staging step, not a claim that the full 41-room Old Pine is playable yet.
